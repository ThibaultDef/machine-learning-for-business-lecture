dvips does not correctly crop/clip eps figures to the bounding box when
\includegraphics* (or \includegraphics[clip]) is used and the eps file is based
on a ps file (e.g. Visio printing to ps, and then converted to eps by
ghostview).
